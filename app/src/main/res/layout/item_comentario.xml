<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/comusuario"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="@dimen/tamano_titulo_comentario"
        android:layout_marginStart="@dimen/margen_titulo_comentario"
        android:layout_marginTop="@dimen/margen_titulo_comentario"
        android:inputType="text"
        android:focusableInTouchMode="false"
        android:clickable="false"
        android:focusable="false"
        android:longClickable="false"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/comtext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margen_comentario"
        android:layout_marginTop="@dimen/margen_entre_comentario"
        android:layout_marginBottom="@dimen/margen_comentario"
        android:layout_marginEnd="@dimen/margen_comentario"
        android:textStyle="italic"
        android:inputType="textMultiLine"
        android:maxLines="8"
        android:textSize="@dimen/tamano_comentario"
        android:focusable="false"
        android:maxLength="@integer/limit_comentario"
        android:focusableInTouchMode="false"
        android:clickable="false"
        android:longClickable="false"
        app:layout_constraintTop_toBottomOf="@id/comusuario"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/comusuario"
        app:layout_constraintEnd_toEndOf="parent" />

</android.support.constraint.ConstraintLayout>